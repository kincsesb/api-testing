{
  "name": "food-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "postinstall": "cd ../food-frontend && npm install && npm run build",
    "start": "node server.js"  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
